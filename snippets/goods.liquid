<div id="goods">
  <div class = "row">
          <div class="col-xs-12 line" id="goods" style="padding-top: 0 !important;"><hr class="symbol-goods"></div>
        </div>
        <div class="row">
          <div class="col-xs-12" style="height:150px !important;"></div>
        </div>

        <div class="row">
        {% for product in collection.products %}
          <!--<div class="col-xs-4" style="border-color:red; border-style: dotted">
           include 'product-loop' with collection.handle 
          </div> end of div class col-xs-6-->
          <div class="col-xs-12 col-sm-4 text-center">
            <a href="{{ url }}">
              <div class="makerproduct">
                <img class="img-responsive" src="https://s3.amazonaws.com/makerproducts/woodandfaulk/Beige_MN_unionwallet.jpg" alt="{{product.title}}" />
              </div>
              <h1 class="makerproducttitle text-center ">{{product.title}}</h1>
              <h2 class="makerproductprice">{{product.price | money }}</h2>
            </a>
          </div>

        {% endfor %}
        </div> <!-- end of div row -->
</div>